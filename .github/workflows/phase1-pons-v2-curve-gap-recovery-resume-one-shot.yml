name: phase1-pons-v2-curve-gap-recovery-resume-one-shot

on:
  push:
    branches:
      - phase1/data-acquisition-spike
    paths:
      - .github/workflows/phase1-pons-v2-curve-gap-recovery-resume-one-shot.yml
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  cancel-oversized:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      GH_TOKEN: ${{ github.token }}
      RUN_ID: "33934557088"
    steps:
      - name: Cancel oversized V2 curve gap recovery if still active
        run: |
          status="$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${RUN_ID}" --jq .status)"
          echo "run ${RUN_ID} status: ${status}"
          if [ "${status}" != "completed" ]; then
            gh api --method POST "repos/${GITHUB_REPOSITORY}/actions/runs/${RUN_ID}/cancel"
          fi

  recover:
    needs: cancel-oversized
    uses: ./.github/workflows/phase1-pons-v2-curve-recover-gaps.yml
    with:
      v2_registry_run_id: "33912235341"
      preserved_run_id: "33912593934"
      partial_recovery_run_id: "33925648297"
      max_gap_blocks: "50000"
    secrets: inherit
