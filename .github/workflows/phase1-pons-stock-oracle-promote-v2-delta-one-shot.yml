name: phase1-pons-stock-oracle-promote-v2-delta-one-shot
# resume-generation: 5

on:
  push:
    branches:
      - phase1/data-acquisition-spike
    paths:
      - .github/workflows/phase1-pons-stock-oracle-promote-v2-delta-one-shot.yml

permissions:
  contents: read
  actions: read

jobs:
  promote:
    if: ${{ contains(github.event.head_commit.message, 'launch stock oracle promotion') }}
    uses: ./.github/workflows/phase1-pons-cbbtc-oracle-promote.yml
    with:
      quote_audit_run_id: "33923299711"
      prior_oracle_run_id: "33974681334"
    secrets: inherit
