name: phase1-pons-viability-pons-v2-transition-one-shot
run-name: phase1 viability pons_v2_transition ${{ github.sha }}

on:
  push:
    branches:
      - phase1/data-acquisition-spike
    paths:
      - .github/workflows/phase1-pons-viability-pons-v2-transition-one-shot.yml

permissions:
  contents: read
  actions: read

concurrency:
  group: phase1-pons-viability-pons-v2-transition-${{ github.ref }}
  cancel-in-progress: false

jobs:
  route:
    if: ${{ contains(github.event.head_commit.message, 'launch viability pons_v2_transition') }}
    uses: ./.github/workflows/phase1-pons-viability-guarded-route.yml
    with:
      route: "pons_v2_transition"
      sequence_id: ${{ github.sha }}
    secrets: inherit
